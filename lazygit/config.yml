git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never --24-bit-color=never
  branchLogCmd: "git log --graph --color=always --abbrev-commit --decorate --date=relative --pretty=medium --oneline --all"
